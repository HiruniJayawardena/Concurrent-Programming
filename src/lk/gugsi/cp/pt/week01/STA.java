package lk.gugsi.cp.pt.week01;

public class STA {

	public static void main(String[] args) {
		System.out.println(Thread.currentThread().getName());
		// If I run this 
		// How many threads will be running in this program
		// Program in execution is the process 
		// one thread
		// There is one process in which there is single thread
		// every java application by default will have one thread
		// created
		// Name of the thread? main
		// on which main method runs
		// only one task can be performed by a single threaded apartment
		
		
	}

}
